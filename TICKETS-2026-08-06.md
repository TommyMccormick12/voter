# Tickets — 2026-08-06 rework

Source: `SPEC-2026-08-06.md`. Every ticket inherits the standing constraints listed there.
Status legend: `[ ]` open, `[x]` done, `[~]` in progress, `[!]` blocked.
Branch: `rework/2026-08-06`. No merge to `main` without Tommy's go.

## P0 — Ballot data

### [ ] T01 — Deactivate the seven confirmed-wrong candidates
Spec A1. Edit `supabase/seed/candidates/*.partial.json`: set `active=false` for marco-rubio, rick-sen-scott, byron-donalds, vernon-buchanan, daniel-webster, anthony-sabatini, alan-mark-grayson.
Gate: confirm each against the DOE qualified list (T02 fetch) before the prod reseed. Reseed prod with the service-role key.
**Accept:** none of the seven active in prod; affected races show "Curating".
Deps: T02 (fetch only, not full rebuild).

### [ ] T02 — DOE roster fetch script + entity spine
Spec A2. New `scripts/ingest/fetch_doe_roster.ts`: POST to `downloadcanlist.asp`, parse tab-delimited, filter federal offices, keep status field. Join Qualified rows to FEC `candidate_id` via `/candidates/search` (state/district/office/cycle=2026). Emit a spine file (`supabase/seed/spine-2026.json`): legal name, office, district (2026 map), party, status, campaign site, FEC id.
**Accept:** spine covers every Qualified federal FL candidate; every row has DOE identity; FEC id present or explicitly null with reason.

### [ ] T03 — Rebuild race + candidate fixtures from the spine
Spec A2. Regenerate race fixtures on the 28-district map from the spine. Carry over existing verified content (stances, votes, money) only where the spine confirms the same person in the same race; drop everything else. Incumbent flags from congress-legislators, unique per race.
**Accept:** spec A2 acceptance; races absent today (FL-11 R Baker/Strada) exist; adversarial verifier passes before prod seed.
Deps: T02.

### [ ] T04 — 2026 district geometry + ZIP precompute
Spec A3. Download EOGPCRP2026 shapefile → GeoJSON (28 polygons) committed under `supabase/seed/geo/`. Intersect Census ZCTAs → `zip-districts-2026.json` (`{zip: [districts]}`). Delete the fake `zip-districts.json`.
**Accept (corrected 2026-08-06):** 33142 → FL-26 majority + FL-24 minority on the new map (audit figure was old-map); 32822 lists FL-09 + FL-10; every FL ZCTA present. T26 verifies 33142 independently.

### [ ] T05 — Address→district API route
Spec A3. `/api/district`: input ZIP (+street address only when the ZIP is split). Census geocoder → lat/lon → turf point-in-polygon on the committed GeoJSON. Never persist or log the address. Handler → application module → adapter flow.
**Accept:** split-ZIP flow resolves; address absent from DB, logs, and analytics; rate-limited like other write APIs.
Deps: T04.

### [ ] T06 — Race-picker uses the new routing
Spec A3/A5. `/race-picker` consumes ZIP precompute + `/api/district`; split ZIPs prompt for street address; non-FL ZIPs keep the honest empty state. Render the no-primary informational state ("No primary — [name] qualified unopposed and advances", scorecard viewable).
Deps: T04, T05, T03 (for no-primary data), C-tickets for states.

### [ ] T07 — Drop Governor
Spec A4. Delete `race-fl-gov-*-2026` fixtures + gov rows; fix README/CLAUDE.md scope contradictions.

## P1 — Pipeline correctness

### [ ] T08 — Names/title-leakage fix
Spec B1.1. `src/lib/api-clients/names.ts`, `scripts/ingest/fetch_fec.ts`. Unit tests for the known bad cases ("Rick Sen Scott", "Scott Mr. Franklin").

### [ ] T09 — Wikidata gate for Wikipedia
Spec B1.2. `fetch_platform.ts`/`wikipedia.ts`: resolve QID with matching P3602/P768 before any page read; no QID = explicit miss. Sanity checks (death date, birth year vs candidacy).

### [ ] T10 — ID-joined votes (Congress.gov + Voteview)
Spec B1.3 + B2. Replace GovTrack scrape + `matchRoleByName`. House via Congress.gov API (bioguide), Senate via Voteview CSVs. Crosswalk via congress-legislators (`id.fec` list rule). Eliminate `vote-undefined` and YEA+NAY contradictions at ingest; fix truncated titles.

### [ ] T11 — FEC money by ID, cycle-pinned
Spec B1.4 + B3. Totals via `/candidate/{id}/totals?cycle=2026`; Senate `election_full` rule; no silent fallback; store + render `coverage_end_date`.

### [ ] T12 — Seed hardening + freshness gate
Spec B4. Abort candidate seed on child-insert failure. Enforce ≥3 stances at activation. Add `verified_at` (migration); stale rows cannot stay active. Activation gate checks candidacy status against the spine.
Deps: T02.

### [ ] T13 — Fixture dedupe + slug resolution
Spec B1 tail. Dedupe byte-identical rows (4 files); resolve the 5 dual-race slugs.
Deps: T03 (rebuild may subsume; verify remainder).

## P1 — App code

### [ ] T14 — Supabase types + two adapters + boundary module
Spec C1. `npx supabase gen types`; `src/lib/data/adapter-anon.ts` + `adapter-service.ts`; one snake_case→camelCase boundary; remove inline clients.

### [ ] T15 — Wire write endpoints + persist matches
Spec C2. `/api/interaction`, `/api/quick-poll`, `/api/visit` → real INSERTs; `/api/match` writes `llm_matches`; `/api/data-rights` reads real rows. Keep consent + rate limits.
Deps: T14.

### [ ] T16 — Typed reads + five states + route error/loading files
Spec C3. Typed results from `data/races.ts`/`data/candidates.ts`; `error.tsx` + `loading.tsx` per route; five states on every data surface.
Deps: T14.

### [ ] T17 — Match results via URL/server state
Spec C4. Results keyed by match id or params; survive refresh/deep-link; sessionStorage transport removed; "estimated match" label on `source: 'mock'`.
Deps: T14, T15 (llm_matches persistence gives the server state).

## P1 — Frontend

### [ ] T18 — Token module + shared primitives
Spec D1. One token module feeds Tailwind, components, `/api/og`; party theming + carousel stay; primitives per `frontend-standards`; mockup HTMLs deleted.

### [ ] T19 — Apply standards across surfaces
Spec D2. Five states, forms, tables, accessibility, responsive on all routes.
Deps: T16, T18.

### [ ] T20 — Save/Share on candidate detail
Spec D3. Wire (Save → `candidate_interactions` saved; Share → existing share flow) or remove. No dead controls.
Deps: T15, T18.

## P2 — Platform

### [ ] T21 — Session consolidation
Spec E1. Delete localStorage path in `src/lib/session.ts`; cookie only; update callers + tests.

### [ ] T22 — CI workflow
Spec E2. `.github/workflows/ci.yml`: lint, typecheck (src + scripts, add script), `vitest run`, `next build` on PR + main.

### [ ] T23 — Risk-selected tests
Spec E3. Contract: `/api/match`, `/api/report`. Unit: `matchCandidates` mock path, `extractJson`/whitelist. Component: match wizard states.
Deps: T15, T16, T17.

## P3 — Hygiene

### [ ] T24 — Env + doc drift
Spec F1. `.env.example` additions; drop `FOLLOWTHEMONEY_API_KEY` from docs; rewrite CLAUDE.md skill-routing to the real skill set.

### [ ] T25 — Repo cleanup
Spec F2. Untrack `.gstack/`; delete scaffold SVGs, `voter-mockups.zip`, phase-1 code paths. DB table drops staged as a separate reviewed migration, not run this session.

## Gates

### [ ] T26 — Adversarial verification of the rebuilt roster
Blind verifier(s) attempt to refute the rebuilt roster against DOE/Ballotpedia/county sources before any prod seed. Failures block. Digest to Tommy after activation.
Deps: T03.

### [ ] T27 — code-review + fix findings
Full-branch review against the standards skills; verified findings fixed or ticketed.
Deps: all above.
