# Handoff — 2026-08-07 (post-rework)

For the next session. The primary is **2026-08-18** (11 days out).

## Where things stand

- The full rework is **merged and deployed** (PR #4, merge commit `9dc3a66`). Code and production database are consistent on the 2026 map.
- Production data was reseeded from the verified roster: **57 races, 165 candidates, 9 active**. Every active candidate passed the activation gate (≥3 stances + DOE-spine candidacy + fresh `verified_at`).
- The seven audit-confirmed false candidates are inactive. The two Governor races and phantom FL-10 R are deleted.
- Both blind adversarial verifications passed after fixes: roster 1:1 against a live FL DOE pull; geometry identical to the official EOGPCRP2026 plan; crosswalk population-weighted.
- Process record: `SPEC-2026-08-06.md`, `TICKETS-2026-08-06.md` (all 27 closed), `supabase/seed/ROSTER-RECONCILIATION-2026-08-06.md` (judgment calls + verification outcomes).

## Next steps, in priority order

### 1. Stance-authoring run (the big one)
- **Why:** only 9 of 165 candidates are active. Every other race shows "Curating". This is the coverage lever before Aug 18.
- **When:** after **Aug 8** — the FEC 12-day pre-primary filings (through Jul 29) land ~Aug 8; pair the run with a money re-pull (`cycle=2026` totals, show `coverage_end_date`).
- **How (Decision 12 + `../AGENT-ORCHESTRATION.md`):** Fable authors and activates solo, but every activation must pass (a) mechanical validation and (b) a blind refutation-first verifier. Tommy gets a post-activation digest.
- **Sources (Decision 5, `DATA-SOURCES-2026-08-06.md` §3):** entity spine first (`supabase/seed/spine-2026.json` — every record must attach to a spine row), then Ballotpedia Candidate Connection public pages (manual pull), spine-seeded campaign sites (`fetch_campaign_site.ts`), GDELT news mining pinned to FL outlets (floridapolitics.com highest yield). Wikipedia only behind the Wikidata gate (`wikidata.ts`, already built).
- **Quick wins:** Luna (FL-13), Lee (FL-15), Franklin (FL-18), Salazar (FL-27) — sitting incumbents gate-refused with 1–2 stances each; they likely reach ≥3 fastest. Then Soto (FL-09), Steube (FL-17), Mast (FL-21) (incumbent-flagged, inactive). Then top-2 candidates per contested race.
- **Activation mechanics:** author stances into the race fixture → `npx tsx scripts/review/activate_candidate.ts --race-id X --slug Y` (enforces the gates, stamps `verified_at`) → `npx tsx scripts/seed/seed_candidates.ts --race-id X`. The 14-day freshness window is `VERIFICATION_FRESHNESS_DAYS` in `scripts/review/activation-gate.ts`.

### 2. Votes re-ingest needs a key
- Set `CONGRESS_GOV_API_KEY` in `.env.local` (free: api.congress.gov/sign-up). Without it `fetch_votes.ts` fails loudly by design. Senate side (Voteview) is keyless.

### 3. Money re-pull (fold into step 1)
- `fetch_fec.ts` by candidate id, `cycle=2026` pinned. Vindman/Nixon figures were stale in the audit; the re-pull after Aug 8 refreshes everyone.

### 4. Migration 012 — staged, NOT applied
- `supabase/migrations/012_drop_phase1_tables.sql` drops `engagement_events`, `issue_rankings`, `candidate_comparisons` (dead phase-1 tables). Destructive. Run only with Tommy's explicit approval AND a verified backup. No urgency.

### 5. Small loose ends
- `candidates.fec_coverage_end_date` is plumbed end-to-end in the UI but has **no DB column yet** — add a migration + populate in `fetch_fec.ts` when convenient; DonorProfile lights up automatically.
- `planning/2026-08-06-audit` branch is fully merged; deletable.
- HB 1-D merits litigation continues — if a court alters the map, the geometry/crosswalk pipeline (`build_zip_crosswalk.ts`) rebuilds from a new shapefile.
- Ballotpedia API sales quote still pending (Decision 5); manual public pages until then.

## Ground rules that carry over

- All 13 decisions in `DECISIONS-2026-08-06.md` stand. Do not re-litigate.
- Voter-visible data goes through the adversarial gate. No exceptions.
- Work on a branch; `main` auto-deploys; merges need Tommy's explicit go.
- The mattpocock `to-spec`/`to-tickets`/`implement`/`handoff` skills are user-invocation-only; the equivalent-by-hand pipeline (spec file → tickets file → agent fan-out per `../AGENT-ORCHESTRATION.md` → code-review skill) worked well.
